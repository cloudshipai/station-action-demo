{
  "name": "posthog",
  "description": "MCP server configuration for PostHog analytics",
  "mcpServers": {
    "posthog": {
      "name": "posthog",
      "description": "PostHog analytics - 43 tools for feature flags, insights, experiments, surveys, error tracking",
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote@latest",
        "https://mcp.posthog.com/sse",
        "--header",
        "Authorization:Bearer {{.POSTHOG_API_KEY}}"
      ]
    }
  }
}
